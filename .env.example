# ─── Required ────────────────────────────────────────────────────────────────
WS_SECRET_KEY=generate-a-strong-random-key-here

# ─── Ports ───────────────────────────────────────────────────────────────────
WS_ADMIN_PORT=8000
# Shield ports are configured per-site in the dashboard (range 8080-8099)

# ─── Logging ─────────────────────────────────────────────────────────────────
WS_LOG_LEVEL=info

# ─── Cookie security ─────────────────────────────────────────────────────────
# Set to true when behind HTTPS (production)
WS_COOKIE_SECURE=false

# ─── SMTP (optional — for invites and password resets) ───────────────────────
# WS_SMTP_HOST=smtp.example.com
# WS_SMTP_PORT=587
# WS_SMTP_USER=your@email.com
# WS_SMTP_PASSWORD=secret
# WS_SMTP_FROM_EMAIL=noreply@example.com
# WS_SMTP_FROM_NAME=FrontWall

# ─── WebAuthn / Passkey (optional — adjust for your domain) ─────────────────
# WS_WEBAUTHN_RP_ID=yourdomain.com
# WS_WEBAUTHN_RP_NAME=FrontWall
# WS_WEBAUTHN_ORIGIN=https://yourdomain.com

# ─── Base URL (used in emails) ──────────────────────────────────────────────
# WS_BASE_URL=https://yourdomain.com
